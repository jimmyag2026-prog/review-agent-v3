# review-agent v3 secrets template.
# Copy to ~/.config/review-agent/secrets.env (user install) OR
#         /etc/review-agent/secrets.env (system install) and chmod 600.
# Never commit a filled-in copy.

DEEPSEEK_API_KEY=
LARK_APP_ID=
LARK_APP_SECRET=
LARK_VERIFICATION_TOKEN=
LARK_ENCRYPT_KEY=
